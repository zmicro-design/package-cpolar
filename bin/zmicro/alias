#!/bin/bash

export PACKAGE_NAME="cpolar"
import "$ZMICRO_PACKAGES_PATH/$PACKAGE_NAME/config"
import "$ZMICRO_PACKAGES_PATH/$PACKAGE_NAME/core"

help() {
  echo "Usage:"
  echo "  zmicro cpolar <args ...>"
  echo ""

  cpolar --help
}

version() {
  cpolar --version
}

core() {
  # context directory
  local context="$ZMICRO_PACKAGES_DATA_PATH/cpolar"
  if [ ! -d "$context" ]; then
    mkdir -p $context
  fi
  cd $context
  # context directory

  case $1 in
  -h | --help | help)
    help
    ;;
  -v | --version | version)
    version
    ;;
  *)
    cpolar "$@"
    ;;
  esac
}

run() {
  core "$@"
}

run "$@"
